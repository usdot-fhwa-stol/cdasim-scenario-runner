services:
  v2xhub:
    networks:
      v2xhub_data_internal:
      v2xhub_app_external:
      sim_net:
        alias:
          - ${V2XHUB_SIM_HOST}
networks:
  sim_net:
    external: true
    name: cdasim_net